---
collections:
  - name: community.docker
    version: "==5.2.0"
  # ufw, used by the hardening role.
  - name: community.general
    version: "==12.6.0"
  # authorized_key, used to trust the Jenkins deploy key on the host.
  - name: ansible.posix
    version: "==2.1.0"
