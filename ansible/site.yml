- name: Install system apps
  hosts: portfolio
  vars_files:
    - vaults/keycloak.yml
    - vaults/komodo.yml
  roles:
    - keycloak
    - komodo
  tags:
    - system-apps

- name: Install personal apps
  hosts: portfolio
  vars_files:
    - vaults/heirloom.yml
  roles:
    - heirloom
  tags:
    - personal-apps
