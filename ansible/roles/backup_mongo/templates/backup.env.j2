# Managed by Ansible — backup_mongo ({{ backup_mongo_name }})
RESTIC_REPOSITORY={{ backup_mongo_restic_repo }}
RESTIC_PASSWORD={{ backup_mongo_restic_password }}
MONGO_USERNAME={{ backup_mongo_username }}
MONGO_PASSWORD={{ backup_mongo_password }}
{% for key, value in (backup_mongo_restic_env | default({})).items() %}
{{ key }}={{ value }}
{% endfor %}
